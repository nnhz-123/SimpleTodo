# Python 依赖
fastapi>=0.100.0
uvicorn>=0.23.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
pytest>=7.0.0
pytest-asyncio>=0.21.0

[project]
name = "simpletodo-backend"
version = "0.1.0"
description = "Simple Todo Backend API"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]